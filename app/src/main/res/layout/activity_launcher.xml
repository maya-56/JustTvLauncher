<?xml version="1.0" encoding="utf-8"?>
<androidx.leanback.widget.BrowseFrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:background="@drawable/background"
    android:layout_height="match_parent"
    tools:ignore="ExtraText">

    <!-- This is your BrowseFragment -->
    <fragment
        android:id="@+id/browse_fragment"
        android:name="crazyboyfeng.justTvLauncher.browse.BrowseFragment"
        android:title="@string/app_name"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- TextView positioned at specific location -->
    <TextView
        android:id="@+id/screen_number_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Your Text"
        android:textSize="53sp"
        android:textStyle="bold"
        android:textColor="@android:color/black"
        android:layout_gravity="top|end"
        android:layout_marginEnd="50dp"
        android:layout_marginTop="90dp" />

</androidx.leanback.widget.BrowseFrameLayout>
